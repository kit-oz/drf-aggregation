from datetime import date, datetime, timedelta

RECORDS = [
    {"group": "group2",
     "integer": 0,
     "float": 0,
     "date": date.today(),
     "datetime": datetime.now(),
     "duration": timedelta(days=1)},

    {"group": "group1",
     "integer": 1,
     "float": 1,
     "date": date.today(),
     "datetime": datetime.now(),
     "duration": timedelta(days=1)},

    {"group": "group1",
     "integer": 2,
     "float": 2,
     "date": date.today(),
     "datetime": datetime.now(),
     "duration": timedelta(days=1)},

    {"group": "group3",
     "integer": 3,
     "float": 3,
     "date": date.today(),
     "datetime": datetime.now(),
     "duration": timedelta(days=1)},

    {"group": "group3",
     "integer": 4,
     "float": 4,
     "date": date.today(),
     "datetime": datetime.now(),
     "duration": timedelta(days=1)},

    {"group": "group3",
     "integer": 5,
     "float": 5,
     "date": date.today(),
     "datetime": datetime.now(),
     "duration": timedelta(days=1)},
]
